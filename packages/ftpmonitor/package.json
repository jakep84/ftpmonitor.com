{
  "name": "ftpmonitor",
  "version": "0.1.2",
  "description": "CLI to diagnose FTP/FTPS/SFTP endpoints (DNS → TCP → Auth → List)",
  "license": "MIT",
  "homepage": "https://ftpmonitor.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jakep84/ftpmonitor.com.git"
  },
  "keywords": [
    "ftp",
    "ftps",
    "sftp",
    "monitoring",
    "healthcheck",
    "cli"
  ],
  "type": "commonjs",
  "bin": {
    "ftpmonitor": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "prepack": "npm run build"
  },
  "dependencies": {
    "basic-ftp": "^5.2.0",
    "ssh2-sftp-client": "^12.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
